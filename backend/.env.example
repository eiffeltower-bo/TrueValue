APP_ENV=local
DATABASE_URL=postgresql://truevalue:truevalue@localhost:5432/truevalue
CORS_ORIGINS=http://localhost:5173

# AI features (lead scoring, matchmaking, valuation). Leave empty to run
# deterministic fallbacks. AI_MODE=offline forces fallback even if key is set.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
AI_MODE=auto
